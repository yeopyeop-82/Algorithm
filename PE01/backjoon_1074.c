#include <stdio.h>

void visit (int row, int col, int n) {

    if (n == 1) {
        
    }
        // visit(i, j, n/2);
        // visit(i, j + n/2, n/2);
        // visit(i + n/2, j, n/2);
        // visit(i + n/2, j + n/2, n/2);
}

int main(void) {
    printf("Hello, world !");
    return 0;
}